# Local development convenience variables (NO SECRETS)
# Copy to .env and adjust as needed. This file is safe to commit.

POSTGRES_DB=devices_db
POSTGRES_USER=devices_user
POSTGRES_PASSWORD=devices_password

# Optional: override Spring datasource URL to point to a different DB
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/devices_db
